# DEV-066 — AI Studio 0.8.1 Release Closure

状态：**PASS — AI STUDIO 0.8.1 RELEASED**

## 发布身份

- Product：`0.8.1`
- Source RC：`78180e2136a39d6b739d086bf5610c90f4e11240`
- Annotated tag：`v0.8.1`
- Tag object：`a032778f3fc4a40ae67f3f9dead148472db5d34b`
- Tag peeled commit：`78180e2136a39d6b739d086bf5610c90f4e11240`
- GitHub Release：`AI Studio 0.8.1 — Production Queue Hotfix`
- Release ID：`RE_kwDOTuxMh84WmWAU`
- Release：<https://github.com/zhangcan001/AI-Studio/releases/tag/v0.8.1>
- `draft=false`，`prerelease=false`，assets：`4`

## 根因与修复

0.8.0 正式安装版的真实 Production Package UAT 发现：generic package batch 已经创建，但没有稳定出现在普通 Production Queue 中，用户无法可靠地聚焦并手动启动它。这是 P1 release-critical 问题；0.8.0 的历史 Release 与 tag 保持不变。

0.8.1 将 Production Package batch 接回既有 ProductionQueue / recovery projection，补齐创建后的可见性、focus、status 与项目入口，并恢复明确的 Manual Start。打开 Queue 不会自动启动生产；没有新增第二队列、第二执行器或直接 `/prompt` 旁路，ComfyUI 仍是正式视频执行引擎。

DEV-061A 的 Project Bulk Import Dry-Run 保留为 `OPTIONAL`；DEV-061B 的 Bulk Production Hardening 保留 partial `COMPLETE/PARTIAL` 真值、created/remaining item IDs、500 items → 5 × 100 batches、restart/retry/offline/frozen-asset 边界。

## 正式 UAT 与构建验证

- 官方安装版 0.8.1 已测试。
- 真实 Production Package inspection 与 create 成功。
- 创建后的 batch 在普通 Production Queue 可见并可手动 Start。
- MiniMax H3 / ComfyUI 真实视频生成成功。
- Portable：启动烟测退出码 `0`。
- NSIS：隔离临时目录静默安装退出码 `0`，安装后 ProductVersion `0.8.1`。
- MSI：Windows Installer 数据库 ProductVersion `0.8.1`，ProductName `AI Studio`。
- Source-only CI `33227945425`：success。

## 回归 Gate

- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`：PASS
- `cargo check --manifest-path src-tauri/Cargo.toml`：PASS
- `cargo test --manifest-path src-tauri/Cargo.toml --all-targets -- --test-threads=1`：PASS，库测试 `697 passed / 0 failed / 1 ignored`，集成测试全部 PASS，真实 ComfyUI 测试按环境约束 ignored。
- `pnpm test`：PASS，`97` files / `382` tests。
- `pnpm exec tsc --noEmit`：PASS
- `pnpm build`：PASS
- `git diff --check`：PASS

## Release assets 与 SHA-256

Release 恰好包含以下四个资产。GitHub 对包含空格的安装包文件名显示为点号形式；哈希与本地构建完全一致。

| Asset | SHA-256 |
| --- | --- |
| `ai-studio.exe` | `1D66E4A52A07468CB8E087F5A9CF03A9559289674273C91FE8B7DA31D5A11CA1` |
| `AI.Studio_0.8.1_x64-setup.exe` | `653D6763AA34DEC6099729077BA9831477043F692D340F73CC2E0221964AD05C` |
| `AI.Studio_0.8.1_x64_en-US.msi` | `B944CAA5F7614EC86DB25A874854246B07CB5666A89609F342537EDC4FEEE5A4` |
| `RELEASE_SHA256_0.8.1.txt` | `87348DA79EDEDF564FEE13C7EAE7488545949EDBE3FCEF4945ED60B7009F45F7` |

四个资产均从 GitHub Release 下载到新的临时目录后再次计算；portable、NSIS、MSI 与本地构建和清单逐一 `REMOTE_HASH_PASS`，远端资产数量为 `4`。

## 兼容性边界

- Migration：`025`；Migration `026` 不存在。
- Backup：`15`。
- Manifest：`2`。
- Narrative / consistency 运行契约仍属于产品 `0.7.0`；本次只发布 0.8.1 Production Queue hotfix，不引入新的数据迁移。
- 已有 0.8.0 用户数据、旧 tag 与旧 Release 未被重写。

## 结论

`DEV-066 RELEASE CLOSURE = PASS`。

`DEV-067_RELEASE_GUARD = UNLOCKED`：v0.8.1 annotated tag 与正式 GitHub Release 均已存在，且四个 Release assets 已完成远端哈希复核。
