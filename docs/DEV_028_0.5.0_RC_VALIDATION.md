# DEV-028 — AI Studio 0.5.0 RC Validation

## 1. Decision

**PASS — Source RC is frozen and the candidate artifacts are reproducible from the recorded Source RC SHA.**

`NEXT: DEV-029 — AI Studio 0.5.0 Final Publication`

No `v0.5.0` tag, GitHub release, or release assets were created by DEV-028.

## 2. Identity and baseline

- Repository: `zhangcan001/AI-Studio`
- Branch: `master`
- DEV-028 start SHA: `9ea04276d2ff5f10676c9a98d3e1d865604b5261`
- Source RC SHA: `02e67cff50f5da1d207478071636af166048820c`
- Source RC commit: `chore: prepare AI Studio 0.5.0 release candidate`
- v0.4.0 peeled SHA: `94918f6322ce690ff7b1630961abb56b8a31ed11`
- Local and remote `v0.5.0` tags: absent
- Pre-build worktree: clean

The formal user database was not used for upgrade validation or live writes. Its SHA256 before and after validation was:

`3370B278577936207043D6711DA94359FD35485E90B4F231653F98B75ACDF9A3`

## 3. Release delta and audit

The parallel audit found no P0 issue. The integrated P1 hardening is limited to:

- frozen snapshot-based ordinary shot retry, including prompt, seed, selected image, and ordered Ref2VA references;
- stage-owned prompt inheritance/update safety and safe legacy fallback for partial stage rows;
- structured Comfy preflight error codes preserved through the production queue;
- transaction-internal normalized duplicate shot-name rejection;
- review and retry controls disabled while the workspace is busy;
- migration 019 direct upgrade coverage and v10 stage-prompt/provenance validation;
- version consistency in the frontend, Cargo manifest/lock, and Tauri configuration.

The architecture audit found one production queue/executor path, no direct frontend `/prompt` submission, no hardcoded ComfyUI installation path, no runtime autoinstall/download path, and no new migration or backup format beyond the requested scope.

## 4. Migration and upgrade evidence

- Fresh migrations 001→019: PASS.
- Repeated migration: PASS.
- Dedicated 018→019 test: PASS; both image/video stage rows are backfilled, empty legacy provenance remains empty, prompt deletion sets provenance NULL, and shot deletion cascades stage rows.
- Project backup tests: 26 passed. v1–v9 restore compatibility and v10 roundtrip passed; v10 preserves image/video stage prompt text and provenance independently.
- Isolated RC upgrade root: `C:\Users\ADMIN\AppData\Local\Temp\AIStudio_050_UPGRADE_20260817_1433`.
- Because no pre-existing v0.4 DB/zip fixture was available, the upgrade smoke used a documented v0.4 source-migration fixture (001–018) seeded with project, shot, prompt provenance, task, snapshot, assets, production batch, and production run data. It was not represented as a historical binary backup.
- RC upgrade result: migration 19, seeded project and shot retained, two stage rows created with `legacy v0.4 prompt` and `prm_v04`/`prv_v04` provenance for both stages, task/snapshot/assets/run data retained. A second RC startup kept the same result.

## 5. Product and regression gates

- Rust final regression after the version bump: **485 passed, 0 failed, 1 ignored**; the ignored test is the explicit live GPU harness.
- Frontend final regression: **52 test files, 169 tests passed**.
- `cargo fmt --all -- --check`: PASS.
- `cargo check`: PASS.
- `pnpm test`: PASS.
- `pnpm build`: PASS.
- Six-shot Project Production Pipeline no-GPU E2E: PASS.
- Bulk import/config, atomic invalid-row handling, duplicate blocking, ordinal append, stage prompt freeze, candidate seed/retry determinism, restart/recovery, backup, and migration coverage: PASS through the final suites and targeted tests.
- Final `git diff --check` against the DEV-028 baseline: PASS.

The user-facing UI was not driven by simulated key presses. RC startup created the minimal default project through the normal backend bootstrap; fresh startup, restart, and persistence were checked directly against isolated databases.

## 6. Version and build evidence

All four required version sources report `0.5.0`:

- `package.json`
- `src-tauri/Cargo.toml`
- `src-tauri/Cargo.lock` package `ai-studio`
- `src-tauri/tauri.conf.json`

Build Info continues to use `CARGO_PKG_VERSION`; the formal build was run once with:

```text
AI_STUDIO_BUILD_COMMIT=02e67cff50f5da1d207478071636af166048820c
pnpm tauri build
```

The release `ai-studio.exe` contains both `0.5.0` and the exact Source RC SHA. The artifact filenames, byte counts, and SHA256 values are recorded in [RELEASE_SHA256_0.5.0.txt](RELEASE_SHA256_0.5.0.txt).

## 7. RC fresh smoke

RC executable: `src-tauri/target/release/ai-studio.exe`

Isolated root: `C:\Users\ADMIN\AppData\Local\Temp\AIStudio_050_FRESH_20260817_1433`

- migration max: 19
- projects after normal startup: 1
- shots: 0
- prompt entries: 0
- production runs: 0
- stage prompt rows: 0
- second startup: same values; persistence/restart PASS

## 8. ComfyUI live gate

Configured runtime:

- PID: `32004`
- root: `D:\ComfyUI-WorkFisher-V2`
- Python: `D:\ComfyUI-WorkFisher-V2\python\python.exe`
- endpoint: `http://127.0.0.1:8188`
- ComfyUI: `0.33.0`
- Python: `3.12.10`
- GPU: `NVIDIA GeForce RTX 5060 Ti`
- `/system_stats`: PASS; one device reported
- `/object_info`: PASS; 4516 nodes, including `KSampler`, `CheckpointLoaderSimple`, and `VAELoader`

One representative backend live harness passed with the isolated copied DB: bulk import → image generation → manual image selection → I2V generation → video selection. Result: **1 passed, 0 failed**, 359.59 seconds. Both generated assets were persisted and the final shot reached `COMPLETED`. No full DEV-023–DEV-027 GPU rerun was performed.

## 9. Candidate status and known limits

- Candidate state: RC only; not published.
- No v0.5.0 tag/release/assets were created.
- Existing compiler dead-code warnings remain; they were outside P0/P1 scope and did not fail the regression.
- The upgrade evidence uses the available source-migration fixture because an authentic pre-existing v0.4 DB/zip fixture was unavailable.
- Temporary isolated smoke roots and their generated evidence remain outside the repository; the formal user DB remains unchanged.
