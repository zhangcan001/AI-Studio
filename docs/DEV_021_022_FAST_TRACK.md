# DEV-021-022 FAST TRACK Validation

Generated: 2026-08-16

## 1. Baseline

- Repository: `zhangcan001/AI-Studio`
- Branch: `master`
- Start SHA: `148ea7d7edacfd02fa2a8106db32cfb8f8c428c6`
- Validated implementation SHA before documentation: `c759509`
- Working tree: clean
- `master` and `origin/master`: synchronized

The published `v0.3.0` tag remains `0c0c9b77bbb951f2f0206ea47478f835c416315b`. The GitHub Release and its assets were not modified.

## 2. DEV-020A Idempotency

- Migration: `017_submission_idempotency.sql`
- Database fields: `tasks.submission_idempotency_key`, `submission_attempt`, `parent_task_id`
- Unique index: `idx_tasks_project_submission_idempotency`
- Retry lineage index: `idx_tasks_parent_task_id`
- Repository lookup: indexed `(project_id, submission_idempotency_key)` query
- Event scan: removed from the normal submission lookup path
- Retry identity: parent task, attempt and submission key persist across reloads
- Regression evidence: repository uniqueness/concurrency, reload lookup and retry lineage tests pass in the full Rust run

## 3. DEV-021 Benchmark 2.0

- Repeat controls: `1 / 3 / 5 / 10`, default `3`
- Seed strategy: fixed seed or explicit fixed seed value
- Candidate identity: workflow ID/version/SHA, Recipe version/SHA, runtime package/profile frozen at experiment creation
- Run persistence: `benchmark_runs`
- Quality persistence: `benchmark_quality_scores`
- Telemetry: queue wait, prepare, submit, Comfy execution, collect and total timings, output size and compiled workflow SHA
- Aggregates: min, median, mean, p95 and max duration; success rate and output size
- Comparability: semantic signature and mismatch diagnostics
- Comparison: fastest, most stable, best quality and best balance recommendations
- Promotion: only a saved benchmark recommendation; no automatic Workflow, Recipe, Runtime Package or production-default mutation
- N+1 safety: benchmark results use batch query/join data rather than a per-run full Task Event scan
- UI: repeat/seed controls, result comparison, quality score and recommendation controls are available in the Benchmark panel

## 4. DEV-022 Production Orchestrator

- `ProductionRun`: fixed linear run persisted with project ownership and terminal state
- Stages: Krea2 image generation → manual asset selection → MiniMax H3 video generation
- Stage items: persisted batch item, task, output asset, source asset, reference index, attempt, frozen values and idempotency key
- Krea2: creates a normal `ProductionBatch` through `ProductionQueueService`
- Selection: only assets generated by the current Krea2 stage are accepted; input order is preserved
- H3: creates a normal `ProductionBatch` through the same queue service and injects selected assets according to Recipe input semantics
- Frozen inputs: workflow/Recipe IDs and values are stored in stage configuration; no duplicate workflow JSON is stored by the Orchestrator
- Lineage: `ProductionRun → ProductionStage → ProductionBatch → ProductionBatchItem → Task → Asset`
- Idempotency: stage trigger gate plus persisted stage batch identity prevents duplicate stage creation in one process and after reload
- Retry: H3 retry creates a new batch/attempt and preserves the failed attempt lineage
- Recovery: state is derived from persisted batch/item/task/output truth; no second executor is introduced
- Cancellation: Run cancellation requests Task cancellation and atomically updates Run/Stage/StageItem terminal state
- Templates: Production Run templates persist project-scoped Krea2/H3 selections and numeric defaults
- UI: minimal Production Run workspace supports create, stage progress, selection, H3 numeric controls and template save/load

## 5. Database

- Latest migration: `018`
- `001–016`: unchanged
- `017`: submission idempotency persistence
- `018`: Benchmark 2.0 and Production Orchestrator tables/indexes
- Fresh database: migration test passes through `001 → 018`
- Upgrade database: migration test passes through `011 → 018` and explicitly covers the `016 → 017 → 018` lineage
- `PRAGMA foreign_keys`: verified enabled (`1`)
- Important indexes: submission idempotency; project/status and project/updated Production Run indexes; stage/run and stage/batch indexes; StageItem task/asset/source-asset indexes; benchmark experiment/candidate indexes
- No `019` migration exists

## 6. Backup

- Backup version: `9`
- Compatibility: fixed fixtures v1, v2, v3, v4 and v5–v8 restore successfully
- v9 round trip: Production Run, three stages, stage items, templates, benchmark runs and quality scores restore successfully
- Production Run restore: project ownership and foreign-key relationships are validated; unsafe active states are restored safely without automatic resubmission
- Lineage restore: batch/task/snapshot/asset IDs and retry/reference lineage are preserved or remapped consistently
- External paths: frozen configuration validation rejects absolute filesystem paths

## 7. E2E

The deterministic no-GPU lifecycle test uses the real migrated SQLite schema, real `ProductionQueueService` construction, real `GenerationService` dependency wiring and real Orchestrator state synchronization. It verifies:

```text
Create ProductionRun
→ Krea2 Stage
→ ProductionBatch
→ Task
→ Image Asset
→ Selection
→ Selected Asset
→ H3 Stage
→ ProductionBatch
→ Task
→ Video Asset
→ ProductionRun SUCCEEDED
```

It also verifies persisted Task/Asset lineage, source-asset linkage and project isolation. The test is intentionally a no-GPU fixture: it does not claim a remote ComfyUI or GPU execution PASS.

Additional safety is covered by the full regression suite: failure-state transitions, partial production failure policies, task cancellation, restart recovery, idempotent submission, ordered REF2VA bindings, v9 backup round trip and referenced-asset delete protection all pass their targeted tests.

## 8. Architecture Invariants

- Production `/prompt` submission remains `GenerationService → Comfy adapter`.
- `ProductionOrchestratorService` does not call ComfyUI or `/prompt` directly.
- `ProductionQueueService` remains the only Production Queue executor.
- Benchmark execution reuses the normal queue/generation path; it has no remote executor.
- No second executor, second queue, multi-GPU path or H3 concurrency was added.
- Runtime package source bytes under `src-tauri/runtime_packages` are unchanged from the start SHA.
- Published v0.3.0 tag, GitHub Release and release assets are unchanged.

## 9. Regression

- `cargo fmt --all -- --check`: PASS
- `cargo check`: PASS
- `cargo test -- --test-threads=1`: **458 passed / 0 failed**
- `pnpm test`: **46 files / 152 tests passed / 0 failed**
- `pnpm build`: PASS
- `git diff --check`: PASS
- `pnpm tauri build`: not run; prohibited for this fast-track validation

The frontend build emits only the existing Vite chunk-size advisory (>500 kB); it is not a build failure. Rust emits non-blocking dead-code warnings for existing benchmark helper structures.

## 10. Performance Safety

- Submission idempotency is enforced by a database unique index and indexed lookup.
- Normal idempotency lookup no longer scans recent Task Events.
- Benchmark result loading uses batch data access and aggregate SQL rather than a per-run event query.
- Production Run view loads stage items in a single query and synchronizes batch truth in a set-based update.
- H3 remains strictly serial through the existing queue/admission controls.

## 11. Commits

- `54083f8 feat: persist generation idempotency identities`
- `e0e45c6 feat: add workflow benchmark 2 comparison`
- `5b13362 feat: add production orchestrator foundation`
- `d8e6fcd feat: persist production run backups`
- `78d0458 feat: add production run workspace`
- `708b2c3 test: cover production orchestration lifecycle`
- `c759509 style: format rust regression assertions`

## 12. Known Issues

- BLOCKER: none for the code/database fast track.
- NON-BLOCKER: GPU/remote ComfyUI Live Validation was not performed in this code-only fast track; the no-GPU lifecycle fixture is explicitly not a live execution claim. The frontend bundle-size advisory and existing dead-code warnings remain non-blocking.
- POST-0.4.0: Production Orchestrator Live Validation, richer production-run UX and productization remain for DEV-023; no automatic execution of that phase was performed.

## 13. Final Decision

**DEV-021-022 FAST TRACK PASS**

## 14. Next Step

**NEXT: DEV-023 — Production Orchestrator Live Validation + 0.4.0 Productization**

STOP. DEV-023 was not started automatically.
