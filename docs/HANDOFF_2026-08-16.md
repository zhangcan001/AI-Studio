# AI Studio 交接文档

更新时间：2026-08-16

这份文档用于新对话快速恢复上下文。新对话开始后，先阅读本文件和 [DEV-021-022 FAST TRACK 验证记录](./DEV_021_022_FAST_TRACK.md)，再执行任何操作。

## 1. 仓库状态

- 仓库：`zhangcan001/AI-Studio`
- 本地路径：`C:\Users\ADMIN\Documents\ChatGPT\AI Studio`
- 分支：`master`
- 当前 HEAD：`e304fa448153cd69e0c59c7906ecce31ce6eaa49`
- `origin/master`：与本地一致
- 工作区：干净
- 用户偏好：每次完成后提交并推送到仓库

禁止使用：

- `git reset --hard`
- `git clean -fd`
- 未经确认覆盖用户本地修改

## 2. 当前产品状态

当前阶段：

```text
DEV-021-022 FAST TRACK PASS
```

当前已经完成 Production Orchestrator foundation：

```text
ProductionRun
→ Krea2 Image Stage
→ ProductionBatch / Queue
→ Task
→ Image Asset
→ 人工选图
→ H3 Video Stage
→ ProductionBatch / Queue
→ Task
→ Video Asset
→ ProductionRun SUCCEEDED
```

正式执行链仍然只有：

```text
ProductionQueueService
→ GenerationService
→ Task
→ Snapshot
→ Asset
→ ComfyUI /prompt
```

Orchestrator 不得直接调用 ComfyUI；不得新增第二 Executor、第二 Queue 或 H3 并发执行器。

## 3. 已完成提交

从本轮起点 `148ea7d7edacfd02fa2a8106db32cfb8f8c428c6` 开始，已完成：

- `54083f8 feat: persist generation idempotency identities`
  - Migration 017
  - Task submission idempotency key、attempt、parent task
  - 数据库唯一索引和重试 lineage
- `e0e45c6 feat: add workflow benchmark 2 comparison`
  - Benchmark 2.0 重复运行、固定种子、遥测、聚合、比较、质量评分和推荐
- `5b13362 feat: add production orchestrator foundation`
  - ProductionRun、三阶段状态机、StageItem、Krea2/H3 队列编排、状态同步、取消、重试
- `d8e6fcd feat: persist production run backups`
  - Backup v9
  - ProductionRun、Stages、StageItems、Templates、Benchmark 数据备份恢复
- `78d0458 feat: add production run workspace`
  - Production Run 最小 UI、H3 时长/宽高字段、模板保存/加载、Migration 审计断言
- `708b2c3 test: cover production orchestration lifecycle`
  - 无 GPU 的真实 SQLite/Queue/Orchestrator 生命周期测试
- `c759509 style: format rust regression assertions`
- `e304fa4 docs: record DEV-021-022 fast track validation`

## 4. 数据库与备份冻结边界

- 最新 migration：`018`
- `001–016`：不可修改
- `017`：submission idempotency
- `018`：Benchmark 2.0 + Production Orchestrator
- `019`：不存在，后续本阶段不要新增
- `BACKUP_VERSION = 9`
- 已验证旧备份：v1、v2、v3、v4、v5、v6、v7、v8
- v9 round-trip：PASS
- 外键：`PRAGMA foreign_keys = 1`

## 5. 发布冻结边界

v0.3.0 已发布并冻结，不得修改：

- Tag `v0.3.0`
- Tag SHA：`0c0c9b77bbb951f2f0206ea47478f835c416315b`
- GitHub Release `v0.3.0`
- Release assets
- `src-tauri/runtime_packages` 中已发布 Runtime Package bytes

当前阶段不要执行：

- `git tag v0.4.0`
- 创建 GitHub Release
- 上传安装包
- `pnpm tauri build`
- GPU Live Validation
- 多 GPU 或 H3 并发

## 6. 最终回归数字

最近一次真实执行结果：

- `cargo fmt --all -- --check`：PASS
- `cargo check`：PASS
- `cargo test -- --test-threads=1`：`458 passed / 0 failed`
- `pnpm test`：`46 files / 152 tests passed`
- `pnpm build`：PASS
- `git diff --check`：PASS
- `pnpm tauri build`：本轮按要求未执行

构建只有非阻塞 Vite chunk size advisory；Rust 只有既有 dead-code warning。

## 7. E2E 证据范围

已通过无 GPU 的数据库生命周期测试，覆盖：

- Create ProductionRun
- Krea2 Stage
- ProductionBatch
- Task
- Image Asset
- Selection / Selected Asset
- H3 Stage
- H3 ProductionBatch
- H3 Task
- Video Asset
- ProductionRun `SUCCEEDED`
- Task / Asset lineage
- project isolation

这不是 ComfyUI/GPU 实机成功证据。真实 GPU / ComfyUI Live Validation应作为下一阶段单独执行，不能把 fixture 测试写成 Live PASS。

## 8. 重要代码位置

- Orchestrator：`src-tauri/src/application/production_orchestrator_service.rs`
- Orchestrator domain：`src-tauri/src/domain/production_run.rs`
- Queue：`src-tauri/src/application/production_queue_service.rs`
- Benchmark：`src-tauri/src/application/workflow_benchmark_service.rs`
- Backup：`src-tauri/src/application/project_backup_service.rs`
- Migration 017：`src-tauri/migrations/017_submission_idempotency.sql`
- Migration 018：`src-tauri/migrations/018_production_orchestrator.sql`
- Production UI：`src/features/production/ProductionRunPanel.tsx`
- 前端入口：`src/features/studio/GenerationStudio.tsx`
- 验证文档：`docs/DEV_021_022_FAST_TRACK.md`

## 9. 已知问题与下一步

当前没有 DEV-021/022 代码阻塞项。

非阻塞事项：

- 尚未做 GPU/ComfyUI Live Validation
- 前端 bundle 存在 chunk size advisory
- 存在少量既有 Rust dead-code warnings

下一阶段仅在用户明确要求时开始：

```text
DEV-023 — Production Orchestrator Live Validation + 0.4.0 Productization
```

不要因为新对话启动就自动执行 DEV-023、GPU 任务、打包、Tag 或 Release。

## 10. 新对话建议开场语

可以直接发送：

```text
继续 AI Studio 项目。请先阅读
docs/HANDOFF_2026-08-16.md
和 docs/DEV_021_022_FAST_TRACK.md。

当前 HEAD 是 e304fa448153cd69e0c59c7906ecce31ce6eaa49，
工作区应保持干净，先不要执行 DEV-023、GPU Live Validation、
新增 migration、Backup v10、Tag 或 Release。
如果需要继续，请先报告当前状态和拟执行范围。
```
