# M3 SHOT BATCH PACK 10 = SOURCE STARTED

Pack 10 begins directly after Pack 09. The active production scope remains Kera2
image keyframes and MiniMax H3 reference-image-to-video only.

The source direction is Shot batch planning and bounded review orchestration on
the existing Production Queue / GenerationService / normal Task / Snapshot /
Asset path. It must not introduce a second Task system, ShotTask, Executor,
direct `/prompt`, or a new runtime. No new migration is approved by this source
note; any future schema change requires a separate immutable migration contract
and backup-version decision.
